--------
This function will allow the user to view the desired directory. The syntax for this command would be "view directory". After the call the user can expect to be requested to enter the path of the directory. And example of this could be "./name_of_directory" to view a directory from root folder or "." to view current directory.
--------