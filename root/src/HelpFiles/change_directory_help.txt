--------
This function will allow the user to change to the desired directory. The syntax for this command would be "change directory". After the call the user can expect to be requested to enter the path of the directory to be changed to. An example of this input would be "./name_of_directory" to change to the directory from root folder.
--------